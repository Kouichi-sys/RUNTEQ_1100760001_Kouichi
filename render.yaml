# Render Blueprint。RenderのダッシュボードでBlueprintとして読み込むと、
# Webサービスとデータベースをまとめて作成できる。
databases:
  - name: nxwitnessviewer-db
    databaseName: nxwitnessviewer
    user: nxwitnessviewer
    plan: free
    # Renderの内部接続はリージョンをまたげないため、
    # webサービスと同じリージョンを明示する(未指定だと既定のoregonになる)
    region: singapore

services:
  - type: web
    name: nxwitnessviewer
    runtime: python
    plan: free
    region: singapore
    buildCommand: "./build.sh"
    startCommand: "gunicorn config.wsgi:application --bind 0.0.0.0:$PORT"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: nxwitnessviewer-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.12.7"
      # 社内LAN外ではNxWitness APIに到達できないため、サンプル映像で動かす
      - key: VIDEO_SOURCE
        value: mock
      # レビュー用デモアカウント。パスワードはRenderの画面で設定する
      - key: DEMO_USER_EMAIL
        value: demo@example.com
      - key: DEMO_USER_NAME
        value: デモユーザー
      - key: DEMO_USER_PASSWORD
        sync: false
